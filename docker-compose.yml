name: reverse-proxy

services:
  reverse-proxy:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: npm run dev
    ports:
      - "80:80"
      - "8080:8080"
    network_mode: bridge
    volumes:
      - var/run/docker.sock:/var/run/docker.sock
      - .:/app